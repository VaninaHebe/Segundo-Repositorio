// for ( inicialización, condición, incremento/decremento)
var x = 0;
for ( x; x < 5; x++) {
    console.log ("El valor de x es: "+x); // Sentencia que se repetirá mientras la variable x cumpla la función x < 5
}